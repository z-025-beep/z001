# 图片素材目录

本目录用于存放各站点图片素材和占位图。

使用方式：
- 将正式图片放入此目录
- 按需要命名，如 `wuxi-1.jpg`、`pushi-1.png`
- 在 `script.js` 中对应热点记录补充 `image` 字段即可

当前默认已提供一张可替换占位图：
- `placeholder-hotspot.svg`

示例：

```js
{
  title: '武溪镇',
  image: 'assets/placeholder-hotspot.svg',
  text: '这里是热点说明文字。'
}
```
